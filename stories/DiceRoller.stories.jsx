import '../ui_kits/app/components/DiceRoller.jsx'

const DiceRoller = window.DiceRoller

export default {
  title: 'Components/DiceRoller',
  component: DiceRoller,
  parameters: { layout: 'fullscreen' }
}

export const Default = { args: {} }
